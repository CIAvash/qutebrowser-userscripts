#!/usr/bin/env raku

use Cro::Uri :decode-percents;

use lib $?FILE.IO.parent.add: 'lib';
use Clipboard;

if %*ENV<QUTE_SELECTED_TEXT> and %*ENV<QUTE_URL> {
    copy_to_clipboard %*ENV<QUTE_SELECTED_TEXT>.trim ~ "\n" ~ decode-percents %*ENV<QUTE_URL>;
}
